#application name is important as through this we are going to pick the values from spring cloud server
spring.application.name=limits-service

# specifying where is the spring cloud server is located
spring.cloud.config.uri=http://localhost:8888

server.port=8080
management.security.enabled=false 

# specifying the profile which can be changed based on the env
#spring.profile.active=qa
#spring-boot.run.profiles=dev

#spring.devtools.add-properties=false
#management.endpoints.web.exposure.include=*